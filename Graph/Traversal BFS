def bfs(adj):
  vis = set()
  n = len(adj)
  for i in range(n):
    if i in vis:
      continue
    q = deque([i])
    while q:
      el = q.pop()
			print(el, end= '')
			for adjel in range(n):
				if adj[el][adjel] and adjel not in vis:
					vis.add(adjel)
					q.appendleft(adjel)
		print()
